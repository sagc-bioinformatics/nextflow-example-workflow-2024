
conda.enabled = true

process {
    // To avoid looking through workdirs all the time, we can use publishDir
    // here it's config rather than a directive, so we do it slightly different
    publishDir = { params.outdir }
    conda = "assets/environment.yml"
}